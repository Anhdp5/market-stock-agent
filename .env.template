# ============================================================
# ZaloPay Stock Intelligence Agent - Environment Configuration
# Copy this file to .env and fill in your values
# ============================================================

# --- IMAP (for reading emails from your Outlook inbox) ---
IMAP_HOST=outlook.office365.com
IMAP_PORT=993
IMAP_USER=anhdp5@vng.com.vn
IMAP_PASSWORD=your-email-password-or-app-password

# --- SMTP (for sending the daily report) ---
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=anhdp5@vng.com.vn
SMTP_PASSWORD=your-email-password-or-app-password

# Note: IMAP_PASSWORD and SMTP_PASSWORD are usually the same.
# If your account has MFA enabled, generate an App Password at:
# https://myaccount.microsoft.com → Security → App passwords

# --- Report Recipient